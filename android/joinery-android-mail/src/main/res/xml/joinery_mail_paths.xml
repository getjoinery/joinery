<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider scope for opened mail attachments (cache only — attachments
     are transient downloads, never durable app data). -->
<paths>
    <cache-path name="mail_attachments" path="mail_attachments/" />
</paths>
