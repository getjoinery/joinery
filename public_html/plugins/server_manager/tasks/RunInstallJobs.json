{
  "name": "Run Install Jobs",
  "description": "Spawns the plane-side install executor to run any queued bootstrap jobs — install_node, and retire_install_password once the machine's agents are admitted — over the provision's sealed install password. The executor is a short-lived detached worker; this task only pokes it and returns.",
  "activate_on_install": true,
  "default_frequency": "every_run",
  "config_fields": {}
}
