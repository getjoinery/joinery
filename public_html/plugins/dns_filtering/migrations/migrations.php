<?php
/**
 * ScrollDaddy Plugin Migrations
 *
 * Tables are created automatically from data class field specifications.
 * Default settings are managed declaratively via plugin.json settings.
 *
 * Historical migrations 001, 002, 003 that seeded settings have been removed;
 * installations that already ran them have the tracking rows in
 * plm_plugin_migrations, which is harmless (prevents re-run of anything that
 * was previously there).
 *
 * This file is intentionally empty — there are no data seeds or indexes to run.
 */

return [];
