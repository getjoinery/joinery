<?php
// Core files (PathHelper, Globalvars, SessionControl) are guaranteed available
require_once(PathHelper::getIncludePath('includes/FormWriterV2Bootstrap.php'));

/**
 * FormWriter implementation for Galactic Tribune theme
 * Uses Bootstrap CSS styling via FormWriterV2Bootstrap
 */
class FormWriter extends FormWriterV2Bootstrap {
    // Uses Bootstrap FormWriter v2 functionality
    // Can add theme-specific form methods here if needed
}
